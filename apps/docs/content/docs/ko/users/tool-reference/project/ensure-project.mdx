---
title: 프로젝트 준비
description: "활성 프로젝트를 재사용하거나 조건에 맞춰 생성/정리해 작업 가능한 상태를 보장합니다."
summary: "활성 프로젝트를 재사용하거나 조건에 맞춰 생성/정리해 작업 가능한 상태를 보장합니다."
---

## 빠른 요약
활성 프로젝트를 재사용하거나 조건에 맞춰 생성/정리해 작업 가능한 상태를 보장합니다.

## 개요
- 도구 이름: `ensure_project`
- 카테고리: 프로젝트
- Strict schema: `true`

## 도구 설명
활성 프로젝트를 재사용하거나 조건에 맞춰 생성/정리해 작업 가능한 상태를 보장합니다.

## 입력 스키마 (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "ensure",
        "delete"
      ]
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        }
      }
    },
    "format": {
      "type": "string",
      "enum": [
        "Java Block/Item",
        "geckolib",
        "animated_java",
        "Generic Model"
      ]
    },
    "name": {
      "type": "string"
    },
    "match": {
      "type": "string",
      "enum": [
        "none",
        "format",
        "name",
        "format_and_name"
      ]
    },
    "onMismatch": {
      "type": "string",
      "enum": [
        "reuse",
        "error",
        "create"
      ]
    },
    "onMissing": {
      "type": "string",
      "enum": [
        "create",
        "error"
      ]
    },
    "confirmDiscard": {
      "type": "boolean"
    },
    "uvPixelsPerBlock": {
      "type": "number"
    },
    "dialog": {
      "type": "object",
      "additionalProperties": true
    },
    "force": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## 필드 레퍼런스
| Field | Type | 필수 | 설명 | 제약 |
| --- | --- | --- | --- | --- |
| `action` | `enum("ensure", "delete")` | ❌ | 요청에서 수행할 동작 모드를 선택합니다. | enum: "ensure", "delete" |
| `target` | `object` | ❌ | 대상 선택 정보를 담는 컨테이너 객체입니다. | additionalProperties=false |
| `target.name` | `string` | ❌ | 매칭에 사용할 대상 이름을 지정합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `format` | `enum("Java Block/Item", "geckolib", "animated_java", "Generic Model")` | ❌ | 모델 또는 내보내기 포맷을 지정합니다. | enum: "Java Block/Item", "geckolib", "animated_java", "Generic Model" |
| `name` | `string` | ❌ | 매칭에 사용할 대상 이름을 지정합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `match` | `enum("none", "format", "name", "format_and_name")` | ❌ | `match` 요청 파라미터입니다. | enum: "none", "format", "name", "format_and_name" |
| `onMismatch` | `enum("reuse", "error", "create")` | ❌ | `onMismatch` 요청 파라미터입니다. | enum: "reuse", "error", "create" |
| `onMissing` | `enum("create", "error")` | ❌ | `onMissing` 요청 파라미터입니다. | enum: "create", "error" |
| `confirmDiscard` | `boolean` | ❌ | 기존 상태 폐기 허용 여부를 확인합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `uvPixelsPerBlock` | `number` | ❌ | 블록당 UV 픽셀 밀도를 설정합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `dialog` | `object` | ❌ | 런타임 UI 다이얼로그 옵션을 전달합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `force` | `boolean` | ❌ | 소프트 안전장치를 우회하고 강제 실행합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `ifRevision` | `string` | ❌ | 낙관적 동시성 제어를 위한 리비전 토큰입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `includeState` | `boolean` | ❌ | 응답에 최신 프로젝트 스냅샷을 포함합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `includeDiff` | `boolean` | ❌ | 응답에 리비전 diff 데이터를 포함합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `diffDetail` | `enum("summary", "full")` | ❌ | diff 포함 시 상세 수준을 제어합니다. | enum: "summary", "full" |

## 최소 요청 예시
```json
{}
```

## 응답 예시
이 응답은 스키매틱 예시입니다. 실제 값과 선택 필드는 프로젝트 상태 및 런타임 버전에 따라 달라질 수 있습니다.

```json
{
  "structuredContent": {
    "action": "created",
    "project": {
      "id": "value",
      "format": "Java Block/Item",
      "name": "value"
    }
  }
}
```

## 운영 노트
- 입력 스키마에 선언된 필드만 전송하세요.
- 응답 예시는 스키매틱이므로 선택 필드는 방어적으로 처리하세요.
- 변경 도구에서 지원되는 경우 `ifRevision`을 사용해 충돌을 줄이세요.
