---
title: Export Trace Log
description: "Input schema and field behavior for export_trace_log."
---

## Overview
- Tool name: `export_trace_log`
- Category: Project
- Strict schema: `true`

## Tool Description
Flushes the trace log to disk (writeFile/export) and returns the resource URI for the in-memory log.

## Input Schema (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "mode": {
      "type": "string",
      "enum": [
        "auto",
        "writeFile",
        "export"
      ]
    },
    "destPath": {
      "type": "string"
    },
    "fileName": {
      "type": "string"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `mode` | `enum("auto", "writeFile", "export")` | ❌ | - | enum: "auto", "writeFile", "export" |
| `destPath` | `string` | ❌ | - | - |
| `fileName` | `string` | ❌ | - | - |

## Minimal Request Example
```json
{}
```

## Response Example
```json
{
  "structuredContent": {}
}
```

## Operational Notes
- Use `ifRevision` for mutation calls when available.
- Do not send fields outside the declared schema.
- Run `validate` before `export` for release safety.
