---
title: 내보내기 및 릴리즈 검증
description: 내보내기 전 검증 게이트를 수행하고 추적 가능한 산출물을 생성합니다.
summary: "내보내기 전 검증 게이트를 수행하고 추적 가능한 산출물을 생성합니다."
---

# 내보내기 및 릴리즈 검증

## 목적
검증 결과를 정리한 뒤 배포 가능한 산출물을 생성합니다.

## 사전 준비
- Blockbench가 설치되어 있고 로컬 플러그인 로드가 가능해야 합니다.
- 저장소 루트에서 플러그인 번들을 빌드해야 합니다 (`npm run build:plugin-desktop`).
- 플러그인 설정에서 MCP 엔드포인트 경로(`/mcp`)를 확인해야 합니다.

## 빠른 시작
1. `project.validate`로 모델 검증을 실행합니다.
2. 이슈를 수정하고 검증을 반복합니다.
3. `project.export`로 최종 산출물을 생성합니다.

## 상세 단계
1. `project.validate`로 모델 검증을 실행합니다.
2. 이슈를 수정하고 검증을 반복합니다.
3. `project.export`로 최종 산출물을 생성합니다.
4. `project.export_trace_log`로 추적 로그를 저장합니다.

## 실패와 복구
- 연결 실패: 엔드포인트 경로, 서비스 실행 상태, 방화벽/권한 설정을 재확인합니다.
- 스키마 오류: 최소 요청으로 재시도하고 클라이언트 스키마 캐시를 갱신합니다.
- 검증 실패: 리포트된 항목을 수정한 뒤 Quick Start부터 다시 실행합니다.

## 관련 문서
- [프로젝트 도구 레퍼런스](/ko/docs/users/tool-reference/project)
- [검증 및 내보내기 문제 해결](/ko/docs/users/troubleshooting/validation-and-export)
