---
title: 큐브 페이스 페인팅
description: "큐브 타깃에 단일 페인팅 연산을 적용합니다."
summary: "큐브 타깃에 단일 페인팅 연산을 적용합니다."
---

## 빠른 요약
큐브 타깃에 단일 페인팅 연산을 적용합니다.

## 개요
- 도구 이름: `paint_faces`
- 카테고리: 텍스처
- Strict schema: `true`

## 도구 설명
큐브 타깃에 단일 페인팅 연산을 적용합니다.

## 입력 스키마 (JSON)
```json
{
  "type": "object",
  "required": [
    "target",
    "op"
  ],
  "additionalProperties": false,
  "anyOf": [
    {
      "type": "object",
      "properties": {
        "coordSpace": {
          "type": "string",
          "enum": [
            "face"
          ]
        }
      }
    },
    {
      "type": "object",
      "required": [
        "coordSpace",
        "width",
        "height"
      ],
      "properties": {
        "coordSpace": {
          "type": "string",
          "enum": [
            "texture"
          ]
        }
      }
    }
  ],
  "properties": {
    "textureId": {
      "type": "string"
    },
    "textureName": {
      "type": "string"
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "cubeId": {
          "type": "string"
        },
        "cubeName": {
          "type": "string"
        },
        "face": {
          "type": "string",
          "enum": [
            "north",
            "south",
            "east",
            "west",
            "up",
            "down"
          ],
          "description": "Optional face filter. Omit to apply to all cube faces."
        }
      }
    },
    "coordSpace": {
      "type": "string",
      "enum": [
        "face",
        "texture"
      ]
    },
    "width": {
      "type": "number"
    },
    "height": {
      "type": "number"
    },
    "op": {
      "type": "object",
      "required": [
        "op"
      ],
      "additionalProperties": false,
      "properties": {
        "op": {
          "type": "string",
          "enum": [
            "set_pixel",
            "fill_rect",
            "draw_rect",
            "draw_line"
          ],
          "description": "Drawing operation. Coordinates are in source-canvas pixels (not UV pixels)."
        },
        "x": {
          "type": "number",
          "description": "X coordinate (pixels)."
        },
        "y": {
          "type": "number",
          "description": "Y coordinate (pixels)."
        },
        "width": {
          "type": "number",
          "description": "Width (pixels)."
        },
        "height": {
          "type": "number",
          "description": "Height (pixels)."
        },
        "x1": {
          "type": "number",
          "description": "Line start X (pixels)."
        },
        "y1": {
          "type": "number",
          "description": "Line start Y (pixels)."
        },
        "x2": {
          "type": "number",
          "description": "Line end X (pixels)."
        },
        "y2": {
          "type": "number",
          "description": "Line end Y (pixels)."
        },
        "color": {
          "type": "string",
          "description": "Color in hex (e.g., \"#ff00aa\" or \"#ff00aaff\")."
        },
        "lineWidth": {
          "type": "number",
          "description": "Stroke width (pixels)."
        },
        "shade": {
          "description": "Fill shading for fill_rect. Defaults to enabled; set false (or enabled:false) to disable. Object values tune the effect.",
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "enabled": {
                  "type": "boolean",
                  "description": "Enable/disable shading."
                },
                "intensity": {
                  "type": "number",
                  "description": "Directional shading intensity (0..1)."
                },
                "edge": {
                  "type": "number",
                  "description": "Edge darkening strength (0..1)."
                },
                "noise": {
                  "type": "number",
                  "description": "Per-pixel tonal noise strength (0..1)."
                },
                "seed": {
                  "type": "number",
                  "description": "Deterministic shading seed."
                },
                "lightDir": {
                  "type": "string",
                  "enum": [
                    "tl_br",
                    "tr_bl",
                    "top_bottom",
                    "left_right"
                  ],
                  "description": "Light direction for directional gradient."
                }
              }
            }
          ]
        }
      }
    },
    "mapping": {
      "type": "string",
      "enum": [
        "stretch",
        "tile"
      ]
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## 필드 레퍼런스
| Field | Type | 필수 | 설명 | 제약 |
| --- | --- | --- | --- | --- |
| `textureId` | `string` | ❌ | `textureId` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `textureName` | `string` | ❌ | `textureName` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `target` | `object` | ✅ | 대상 선택 정보를 담는 컨테이너 객체입니다. | additionalProperties=false |
| `target.cubeId` | `string` | ❌ | `target.cubeId` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `target.cubeName` | `string` | ❌ | `target.cubeName` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `target.face` | `enum("north", "south", "east", "west", "up", "down")` | ❌ | Optional face filter. Omit to apply to all cube faces. | enum: "north", "south", "east", "west", "up", "down" |
| `coordSpace` | `enum("face", "texture")` | ❌ | `coordSpace` 요청 파라미터입니다. | enum: "face", "texture" |
| `width` | `number` | ❌ | `width` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `height` | `number` | ❌ | `height` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `op` | `object` | ✅ | `op` 요청 파라미터입니다. | additionalProperties=false |
| `op.op` | `enum("set_pixel", "fill_rect", "draw_rect", "draw_line")` | ✅ | Drawing operation. Coordinates are in source-canvas pixels (not UV pixels). | enum: "set_pixel", "fill_rect", "draw_rect", "draw_line" |
| `op.x` | `number` | ❌ | X coordinate (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.y` | `number` | ❌ | Y coordinate (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.width` | `number` | ❌ | Width (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.height` | `number` | ❌ | Height (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.x1` | `number` | ❌ | Line start X (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.y1` | `number` | ❌ | Line start Y (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.x2` | `number` | ❌ | Line end X (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.y2` | `number` | ❌ | Line end Y (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.color` | `string` | ❌ | Color in hex (e.g., "#ff00aa" or "#ff00aaff"). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.lineWidth` | `number` | ❌ | Stroke width (pixels). | 선언된 타입 외 추가 제약은 없습니다. |
| `op.shade` | `boolean \| object` | ❌ | Fill shading for fill_rect. Defaults to enabled; set false (or enabled:false) to disable. Object values tune the effect. | anyOf(2) |
| `mapping` | `enum("stretch", "tile")` | ❌ | `mapping` 요청 파라미터입니다. | enum: "stretch", "tile" |
| `ifRevision` | `string` | ❌ | 낙관적 동시성 제어를 위한 리비전 토큰입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `includeState` | `boolean` | ❌ | 응답에 최신 프로젝트 스냅샷을 포함합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `includeDiff` | `boolean` | ❌ | 응답에 리비전 diff 데이터를 포함합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `diffDetail` | `enum("summary", "full")` | ❌ | diff 포함 시 상세 수준을 제어합니다. | enum: "summary", "full" |

## 조건 규칙
### `anyOf`
```json
[
  {
    "type": "object",
    "properties": {
      "coordSpace": {
        "type": "string",
        "enum": [
          "face"
        ]
      }
    }
  },
  {
    "type": "object",
    "required": [
      "coordSpace",
      "width",
      "height"
    ],
    "properties": {
      "coordSpace": {
        "type": "string",
        "enum": [
          "texture"
        ]
      }
    }
  }
]
```

## 최소 요청 예시
```json
{
  "target": {
    "cubeName": "cube"
  },
  "op": {
    "op": "set_pixel"
  }
}
```

## 응답 예시
이 응답은 스키매틱 예시입니다. 실제 값과 선택 필드는 프로젝트 상태 및 런타임 버전에 따라 달라질 수 있습니다.

```json
{
  "structuredContent": {
    "textureName": "value",
    "width": 0,
    "height": 0,
    "targets": 0
  }
}
```

## 운영 노트
- 입력 스키마에 선언된 필드만 전송하세요.
- 응답 예시는 스키매틱이므로 선택 필드는 방어적으로 처리하세요.
- 변경 도구에서 지원되는 경우 `ifRevision`을 사용해 충돌을 줄이세요.
