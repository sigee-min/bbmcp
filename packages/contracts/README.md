# @ashfox/contracts

Contract source-of-truth package for Ashfox.

Current scope:
- MCP JSON schema contracts (`mcpSchemas/*`)
- Schema validation helpers used by transport/runtime
- Schema policy constants (`mcpSchemas/policy`) for version and registry hash rules
