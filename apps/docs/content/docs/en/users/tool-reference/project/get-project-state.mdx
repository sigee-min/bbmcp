---
title: Get Project State
description: "Input schema and field behavior for get_project_state."
---

## Overview
- Tool name: `get_project_state`
- Category: Project
- Strict schema: `true`

## Tool Description
Returns the current project state (summary by default). Summary includes texture metadata and textureResolution. Full detail includes textureUsage (per-face mappings) when available.

## Input Schema (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "detail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    },
    "includeUsage": {
      "type": "boolean",
      "description": "Include textureUsage in the response (defaults to true when detail=full)."
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `detail` | `enum("summary", "full")` | ❌ | - | enum: "summary", "full" |
| `includeUsage` | `boolean` | ❌ | Include textureUsage in the response (defaults to true when detail=full). | - |

## Minimal Request Example
```json
{}
```

## Response Example
```json
{
  "structuredContent": {
    "project": {
      "id": "value",
      "active": false,
      "name": "value",
      "format": "Java Block/Item",
      "revision": "value",
      "counts": {
        "bones": 0,
        "cubes": 0,
        "textures": 0,
        "animations": 0
      }
    }
  }
}
```

## Operational Notes
- Use `ifRevision` for mutation calls when available.
- Do not send fields outside the declared schema.
- Run `validate` before `export` for release safety.
