---
title: 트레이스 로그 내보내기
description: "트레이스 로그 내보내기 도구의 입력 스펙과 필드 동작 설명."
---

## 개요
- 도구 이름: `export_trace_log`
- 카테고리: 프로젝트
- Strict schema: `true`

## 도구 설명
Flushes the trace log to disk (writeFile/export) and returns the resource URI for the in-memory log.

## 입력 스키마 (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "mode": {
      "type": "string",
      "enum": [
        "auto",
        "writeFile",
        "export"
      ]
    },
    "destPath": {
      "type": "string"
    },
    "fileName": {
      "type": "string"
    }
  }
}
```

## 필드 레퍼런스
| Field | Type | 필수 | Description | Constraints |
| --- | --- | --- | --- | --- |
| `mode` | `enum("auto", "writeFile", "export")` | ❌ | - | enum: "auto", "writeFile", "export" |
| `destPath` | `string` | ❌ | - | - |
| `fileName` | `string` | ❌ | - | - |

## 최소 요청 예시
```json
{}
```

## 응답 예시
```json
{
  "structuredContent": {}
}
```

## 운영 노트
- 변경 도구는 `ifRevision` 사용을 권장합니다.
- 스키마에 없는 필드는 보내지 마세요 (strict schema).
- `validate`로 최종 상태를 확인한 후 `export`를 수행하세요.
