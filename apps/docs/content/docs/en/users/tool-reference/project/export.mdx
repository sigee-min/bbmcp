---
title: Export
description: "Exports the current project to the requested output format (including native codec targets like glTF/OBJ/FBX when available)."
summary: "Exports the current project to the requested output format (including native codec targets like glTF/OBJ/FBX when available)."
---

## Quick Summary
Exports the current project to the requested output format (including native codec targets like glTF/OBJ/FBX when available).

## Overview
- Tool name: `export`
- Category: Project
- Strict schema: `true`

## Tool Description
Exports the current project to the requested output format (including native codec targets like glTF/OBJ/FBX when available).

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "format",
    "destPath"
  ],
  "additionalProperties": false,
  "properties": {
    "format": {
      "enum": [
        "java_block_item_json",
        "gecko_geo_anim",
        "animated_java",
        "generic_model_json",
        "gltf",
        "native_codec"
      ]
    },
    "codecId": {
      "type": "string"
    },
    "destPath": {
      "type": "string"
    },
    "options": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "fallback": {
          "type": "string",
          "enum": [
            "strict",
            "best_effort"
          ]
        },
        "includeDiagnostics": {
          "type": "boolean"
        }
      }
    },
    "includeState": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `format` | `enum("java_block_item_json", "gecko_geo_anim", "animated_java", "generic_model_json", "gltf", "native_codec")` | ✅ | Specifies the model or export format. | enum: "java_block_item_json", "gecko_geo_anim", "animated_java", "generic_model_json", "gltf", "native_codec" |
| `codecId` | `string` | ❌ | Request parameter `codecId`. | No extra constraints beyond the declared type. |
| `destPath` | `string` | ✅ | Destination path for writing output artifacts. | No extra constraints beyond the declared type. |
| `options` | `object` | ❌ | Request parameter `options`. | additionalProperties=false |
| `options.fallback` | `enum("strict", "best_effort")` | ❌ | Request parameter `options.fallback`. | enum: "strict", "best_effort" |
| `options.includeDiagnostics` | `boolean` | ❌ | Request parameter `options.includeDiagnostics`. | No extra constraints beyond the declared type. |
| `includeState` | `boolean` | ❌ | Includes the latest project snapshot in the response. | No extra constraints beyond the declared type. |
| `ifRevision` | `string` | ❌ | Optimistic concurrency token used to avoid stale writes. | No extra constraints beyond the declared type. |

## Minimal Request Example
```json
{
  "format": "java_block_item_json",
  "destPath": "./out/model.json"
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "path": "value"
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat response examples as schematic and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
