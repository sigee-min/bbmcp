---
title: Ensure Project
description: "Ensures a usable project. Reuses the active project by default and can create a new one when missing or on mismatch (per options). Use match/onMismatch/onMissing to control behavior. action=\"delete\" closes the active project (requires target.name)."
summary: "Ensures a usable project. Reuses the active project by default and can create a new one when missing or on mismatch (per options). Use match/onMismatch/onMissing to control behavior. action=\\\"delete\\\" closes the..."
---

## Quick Summary
Ensures a usable project. Reuses the active project by default and can create a new one when missing or on mismatch (per options). Use match/onMismatch/onMissing to control behavior. action="delete" closes the active project (requires target.name).

## Overview
- Tool name: `ensure_project`
- Category: Project
- Strict schema: `true`

## Tool Description
Ensures a usable project. Reuses the active project by default and can create a new one when missing or on mismatch (per options). Use match/onMismatch/onMissing to control behavior. action="delete" closes the active project (requires target.name).

## Input Schema (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "ensure",
        "delete"
      ]
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        }
      }
    },
    "format": {
      "type": "string",
      "enum": [
        "Java Block/Item",
        "geckolib",
        "animated_java",
        "Generic Model"
      ]
    },
    "name": {
      "type": "string"
    },
    "match": {
      "type": "string",
      "enum": [
        "none",
        "format",
        "name",
        "format_and_name"
      ]
    },
    "onMismatch": {
      "type": "string",
      "enum": [
        "reuse",
        "error",
        "create"
      ]
    },
    "onMissing": {
      "type": "string",
      "enum": [
        "create",
        "error"
      ]
    },
    "confirmDiscard": {
      "type": "boolean"
    },
    "uvPixelsPerBlock": {
      "type": "number"
    },
    "dialog": {
      "type": "object",
      "additionalProperties": true
    },
    "force": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `action` | `enum("ensure", "delete")` | ❌ | Selects the operation mode for this request. | enum: "ensure", "delete" |
| `target` | `object` | ❌ | Container object describing the target selector. | additionalProperties=false |
| `target.name` | `string` | ❌ | Specifies the target name used for matching. | No extra constraints beyond the declared type. |
| `format` | `enum("Java Block/Item", "geckolib", "animated_java", "Generic Model")` | ❌ | Specifies the model or export format. | enum: "Java Block/Item", "geckolib", "animated_java", "Generic Model" |
| `name` | `string` | ❌ | Specifies the target name used for matching. | No extra constraints beyond the declared type. |
| `match` | `enum("none", "format", "name", "format_and_name")` | ❌ | Request parameter `match`. | enum: "none", "format", "name", "format_and_name" |
| `onMismatch` | `enum("reuse", "error", "create")` | ❌ | Request parameter `onMismatch`. | enum: "reuse", "error", "create" |
| `onMissing` | `enum("create", "error")` | ❌ | Request parameter `onMissing`. | enum: "create", "error" |
| `confirmDiscard` | `boolean` | ❌ | Confirms that existing state can be discarded. | No extra constraints beyond the declared type. |
| `uvPixelsPerBlock` | `number` | ❌ | Sets UV pixel density per block. | No extra constraints beyond the declared type. |
| `dialog` | `object` | ❌ | Additional UI dialog options passed to runtime. | No extra constraints beyond the declared type. |
| `force` | `boolean` | ❌ | Forces operation by bypassing soft safety checks. | No extra constraints beyond the declared type. |
| `ifRevision` | `string` | ❌ | Optimistic concurrency token used to avoid stale writes. | No extra constraints beyond the declared type. |
| `includeState` | `boolean` | ❌ | Includes the latest project snapshot in the response. | No extra constraints beyond the declared type. |
| `includeDiff` | `boolean` | ❌ | Includes revision diff data in the response. | No extra constraints beyond the declared type. |
| `diffDetail` | `enum("summary", "full")` | ❌ | Controls diff verbosity when diff data is included. | enum: "summary", "full" |

## Minimal Request Example
```json
{}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "action": "created",
    "project": {
      "id": "value",
      "format": "Java Block/Item",
      "name": "value"
    }
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat response examples as schematic and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
