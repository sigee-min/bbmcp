---
title: Reload Plugins
description: "Input schema and field behavior for reload_plugins."
---

## Overview
- Tool name: `reload_plugins`
- Category: System
- Strict schema: `true`

## Tool Description
Reloads Blockbench plugins (confirm required).

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "confirm"
  ],
  "additionalProperties": false,
  "properties": {
    "confirm": {
      "type": "boolean"
    },
    "delayMs": {
      "type": "number"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `confirm` | `boolean` | ✅ | - | - |
| `delayMs` | `number` | ❌ | - | - |

## Minimal Request Example
```json
{
  "confirm": true
}
```

## Response Example
```json
{
  "structuredContent": {
    "scheduled": true,
    "delayMs": 0,
    "method": "devReload"
  }
}
```

## Operational Notes
- Use `ifRevision` for mutation calls when available.
- Do not send fields outside the declared schema.
- Run `validate` before `export` for release safety.
