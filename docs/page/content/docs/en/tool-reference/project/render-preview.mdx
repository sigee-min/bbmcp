---
title: Render Preview
description: "Input schema and field behavior for render_preview."
---

## Overview
- Tool name: `render_preview`
- Category: Project
- Strict schema: `true`

## Tool Description
Renders a preview image. fixed -> single (optional angle). turntable -> sequence. Returns MCP image content blocks (base64 PNG) plus structured metadata without dataUri. Set saveToTmp=true to write snapshots into .ashfox/tmp for manual upload fallback. See ashfox://guide/vision-fallback via resources/read. Single returns result.image; sequence returns result.frames[]. Example(single): \{"mode":"fixed","output":"single","angle":[30,45,0]\} Example(sequence): \{"mode":"turntable","output":"sequence","durationSeconds":2,"fps":12\}

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "mode"
  ],
  "additionalProperties": false,
  "properties": {
    "mode": {
      "enum": [
        "fixed",
        "turntable"
      ]
    },
    "angle": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 2,
      "maxItems": 3
    },
    "clip": {
      "type": "string"
    },
    "timeSeconds": {
      "type": "number"
    },
    "durationSeconds": {
      "type": "number"
    },
    "fps": {
      "type": "number"
    },
    "output": {
      "enum": [
        "single",
        "sequence"
      ]
    },
    "saveToTmp": {
      "type": "boolean"
    },
    "tmpName": {
      "type": "string"
    },
    "tmpPrefix": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `mode` | `enum("fixed", "turntable")` | ✅ | - | enum: "fixed", "turntable" |
| `angle` | `array<number>` | ❌ | - | minItems: 2, maxItems: 3 |
| `clip` | `string` | ❌ | - | - |
| `timeSeconds` | `number` | ❌ | - | - |
| `durationSeconds` | `number` | ❌ | - | - |
| `fps` | `number` | ❌ | - | - |
| `output` | `enum("single", "sequence")` | ❌ | - | enum: "single", "sequence" |
| `saveToTmp` | `boolean` | ❌ | - | - |
| `tmpName` | `string` | ❌ | - | - |
| `tmpPrefix` | `string` | ❌ | - | - |
| `includeState` | `boolean` | ❌ | - | - |
| `ifRevision` | `string` | ❌ | - | - |

## Minimal Request Example
```json
{
  "mode": "fixed"
}
```

## Response Example
```json
{
  "structuredContent": {
    "kind": "single",
    "frameCount": 0
  }
}
```

## Operational Notes
- Use `ifRevision` for mutation calls when available.
- Do not send fields outside the declared schema.
- Run `validate` before `export` for release safety.

