---
title: 프로젝트 준비
description: "프로젝트 준비 도구의 입력 스펙과 필드 동작 설명."
---

## 개요
- 도구 이름: `ensure_project`
- 카테고리: 프로젝트
- Strict schema: `true`

## 도구 설명
Ensures a usable project. Reuses the active project by default and can create a new one when missing or on mismatch (per options). Use match/onMismatch/onMissing to control behavior. action="delete" closes the active project (requires target.name).

## 입력 스키마 (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "ensure",
        "delete"
      ]
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        }
      }
    },
    "format": {
      "type": "string",
      "enum": [
        "Java Block/Item",
        "geckolib",
        "animated_java",
        "Image",
        "Generic Model"
      ]
    },
    "name": {
      "type": "string"
    },
    "match": {
      "type": "string",
      "enum": [
        "none",
        "format",
        "name",
        "format_and_name"
      ]
    },
    "onMismatch": {
      "type": "string",
      "enum": [
        "reuse",
        "error",
        "create"
      ]
    },
    "onMissing": {
      "type": "string",
      "enum": [
        "create",
        "error"
      ]
    },
    "confirmDiscard": {
      "type": "boolean"
    },
    "uvPixelsPerBlock": {
      "type": "number"
    },
    "dialog": {
      "type": "object",
      "additionalProperties": true
    },
    "force": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## 필드 레퍼런스
| Field | Type | 필수 | Description | Constraints |
| --- | --- | --- | --- | --- |
| `action` | `enum("ensure", "delete")` | ❌ | - | enum: "ensure", "delete" |
| `target` | `object` | ❌ | - | additionalProperties=false |
| `target.name` | `string` | ❌ | - | - |
| `format` | `enum("Java Block/Item", "geckolib", "animated_java", "Image", "Generic Model")` | ❌ | - | enum: "Java Block/Item", "geckolib", "animated_java", "Image", "Generic Model" |
| `name` | `string` | ❌ | - | - |
| `match` | `enum("none", "format", "name", "format_and_name")` | ❌ | - | enum: "none", "format", "name", "format_and_name" |
| `onMismatch` | `enum("reuse", "error", "create")` | ❌ | - | enum: "reuse", "error", "create" |
| `onMissing` | `enum("create", "error")` | ❌ | - | enum: "create", "error" |
| `confirmDiscard` | `boolean` | ❌ | - | - |
| `uvPixelsPerBlock` | `number` | ❌ | - | - |
| `dialog` | `object` | ❌ | - | - |
| `force` | `boolean` | ❌ | - | - |
| `ifRevision` | `string` | ❌ | - | - |
| `includeState` | `boolean` | ❌ | - | - |
| `includeDiff` | `boolean` | ❌ | - | - |
| `diffDetail` | `enum("summary", "full")` | ❌ | - | enum: "summary", "full" |

## 최소 요청 예시
```json
{}
```

## 응답 예시
```json
{
  "structuredContent": {
    "action": "created",
    "project": {
      "id": "value",
      "format": "Java Block/Item",
      "name": "value"
    }
  }
}
```

## 운영 노트
- 변경 도구는 `ifRevision` 사용을 권장합니다.
- 스키마에 없는 필드는 보내지 마세요 (strict schema).
- `validate`로 최종 상태를 확인한 후 `export`를 수행하세요.
