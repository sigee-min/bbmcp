---
title: List Capabilities
description: "Input schema and field behavior for list_capabilities."
---

## Overview
- Tool name: `list_capabilities`
- Category: System
- Strict schema: `true`

## Tool Description
Returns plugin capabilities and limits. Tool schemas are strict (extra fields are rejected).

## Input Schema (JSON)
```json
{
  "type": "object",
  "additionalProperties": false
}
```

## Field Reference
This tool has no input fields.

## Minimal Request Example
```json
{}
```

## Response Example
```json
{
  "structuredContent": {
    "pluginVersion": "value",
    "blockbenchVersion": "value",
    "formats": [
      {
        "format": "Java Block/Item",
        "animations": false,
        "enabled": false
      }
    ],
    "limits": {
      "maxCubes": 0,
      "maxTextureSize": 0,
      "maxAnimationSeconds": 0
    }
  }
}
```

## Operational Notes
- Use `ifRevision` for mutation calls when available.
- Do not send fields outside the declared schema.
- Run `validate` before `export` for release safety.
