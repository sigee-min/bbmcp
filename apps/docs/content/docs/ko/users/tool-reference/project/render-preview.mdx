---
title: 프리뷰 렌더링
description: "현재 프로젝트를 렌더링해 시각 결과를 빠르게 확인합니다."
summary: "현재 프로젝트를 렌더링해 시각 결과를 빠르게 확인합니다."
---

## 빠른 요약
현재 프로젝트를 렌더링해 시각 결과를 빠르게 확인합니다.

## 개요
- 도구 이름: `render_preview`
- 카테고리: 프로젝트
- Strict schema: `true`

## 도구 설명
현재 프로젝트를 렌더링해 시각 결과를 빠르게 확인합니다.

## 입력 스키마 (JSON)
```json
{
  "type": "object",
  "required": [
    "mode"
  ],
  "additionalProperties": false,
  "properties": {
    "mode": {
      "enum": [
        "fixed",
        "turntable"
      ]
    },
    "angle": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 2,
      "maxItems": 3
    },
    "clip": {
      "type": "string"
    },
    "timeSeconds": {
      "type": "number"
    },
    "durationSeconds": {
      "type": "number"
    },
    "fps": {
      "type": "number"
    },
    "output": {
      "enum": [
        "single",
        "sequence"
      ]
    },
    "saveToTmp": {
      "type": "boolean"
    },
    "tmpName": {
      "type": "string"
    },
    "tmpPrefix": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    }
  }
}
```

## 필드 레퍼런스
| Field | Type | 필수 | 설명 | 제약 |
| --- | --- | --- | --- | --- |
| `mode` | `enum("fixed", "turntable")` | ✅ | 도구 실행 방식을 선택합니다. | enum: "fixed", "turntable" |
| `angle` | `array<number>` | ❌ | `angle` 요청 파라미터입니다. | minItems: 2, maxItems: 3 |
| `clip` | `string` | ❌ | 작업 대상 애니메이션 클립 이름입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `timeSeconds` | `number` | ❌ | `timeSeconds` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `durationSeconds` | `number` | ❌ | `durationSeconds` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `fps` | `number` | ❌ | `fps` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `output` | `enum("single", "sequence")` | ❌ | `output` 요청 파라미터입니다. | enum: "single", "sequence" |
| `saveToTmp` | `boolean` | ❌ | `saveToTmp` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `tmpName` | `string` | ❌ | `tmpName` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `tmpPrefix` | `string` | ❌ | `tmpPrefix` 요청 파라미터입니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `includeState` | `boolean` | ❌ | 응답에 최신 프로젝트 스냅샷을 포함합니다. | 선언된 타입 외 추가 제약은 없습니다. |
| `ifRevision` | `string` | ❌ | 낙관적 동시성 제어를 위한 리비전 토큰입니다. | 선언된 타입 외 추가 제약은 없습니다. |

## 최소 요청 예시
```json
{
  "mode": "fixed"
}
```

## 응답 예시
이 응답은 스키매틱 예시입니다. 실제 값과 선택 필드는 프로젝트 상태 및 런타임 버전에 따라 달라질 수 있습니다.

```json
{
  "structuredContent": {
    "kind": "single",
    "frameCount": 0
  }
}
```

## 운영 노트
- 입력 스키마에 선언된 필드만 전송하세요.
- 응답 예시는 스키매틱이므로 선택 필드는 방어적으로 처리하세요.
- 이 도구는 읽기 전용이므로 일반적으로 `ifRevision`이 필요하지 않습니다.
