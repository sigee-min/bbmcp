---
title: Set Trigger Keyframes
description: "Sets trigger keyframes (sound/particle/timeline), one key per call."
summary: "Sets trigger keyframes (sound/particle/timeline), one key per call."
---

## Quick Summary
Sets trigger keyframes (sound/particle/timeline), one key per call.

## Overview
- Tool name: `set_trigger_keyframes`
- Category: Animation
- Strict schema: `true`

## Tool Description
Sets trigger keyframes (sound/particle/timeline), one key per call.

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "clip",
    "channel",
    "keys"
  ],
  "additionalProperties": false,
  "properties": {
    "clipId": {
      "type": "string"
    },
    "clip": {
      "type": "string"
    },
    "channel": {
      "type": "string",
      "enum": [
        "sound",
        "particle",
        "timeline"
      ]
    },
    "keys": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": {
        "type": "object",
        "required": [
          "time",
          "value"
        ],
        "additionalProperties": false,
        "properties": {
          "time": {
            "type": "number"
          },
          "value": {}
        }
      }
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `clipId` | `string` | ❌ | Animation clip identifier used as the target. | No extra constraints beyond the declared type. |
| `clip` | `string` | ✅ | Animation clip name used as the target. | No extra constraints beyond the declared type. |
| `channel` | `enum("sound", "particle", "timeline")` | ✅ | Request parameter `channel`. | enum: "sound", "particle", "timeline" |
| `keys` | `array<object>` | ✅ | Request parameter `keys`. | minItems: 1, maxItems: 1 |
| `keys[].time` | `number` | ✅ | Request parameter `keys[].time`. | No extra constraints beyond the declared type. |
| `keys[].value` | `any` | ✅ | Request parameter `keys[].value`. | No extra constraints beyond the declared type. |
| `ifRevision` | `string` | ❌ | Optimistic concurrency token used to avoid stale writes. | No extra constraints beyond the declared type. |
| `includeState` | `boolean` | ❌ | Includes the latest project snapshot in the response. | No extra constraints beyond the declared type. |
| `includeDiff` | `boolean` | ❌ | Includes revision diff data in the response. | No extra constraints beyond the declared type. |
| `diffDetail` | `enum("summary", "full")` | ❌ | Controls diff verbosity when diff data is included. | enum: "summary", "full" |

## Minimal Request Example
```json
{
  "clip": "clip",
  "channel": "timeline",
  "keys": [
    {
      "time": 0,
      "value": "marker"
    }
  ]
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "clip": "value",
    "channel": "value"
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat response examples as schematic and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
