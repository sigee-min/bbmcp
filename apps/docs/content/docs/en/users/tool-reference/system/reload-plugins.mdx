---
title: Reload Plugins
description: "Reloads Blockbench plugins (confirm required)."
summary: "Reloads Blockbench plugins (confirm required)."
---

## Quick Summary
Reloads Blockbench plugins (confirm required).

## Overview
- Tool name: `reload_plugins`
- Category: System
- Strict schema: `true`

## Tool Description
Reloads Blockbench plugins (confirm required).

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "confirm"
  ],
  "additionalProperties": false,
  "properties": {
    "confirm": {
      "type": "boolean"
    },
    "delayMs": {
      "type": "number"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `confirm` | `boolean` | ✅ | Explicit confirmation flag for disruptive actions. | No extra constraints beyond the declared type. |
| `delayMs` | `number` | ❌ | Request parameter `delayMs`. | No extra constraints beyond the declared type. |

## Minimal Request Example
```json
{
  "confirm": true
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "scheduled": true,
    "delayMs": 0,
    "method": "devReload"
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat response examples as schematic and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
