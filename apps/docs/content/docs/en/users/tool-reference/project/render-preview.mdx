---
title: Render Preview
description: "Renders a preview image. fixed -> single (optional angle). turntable -> sequence. Returns MCP image content blocks (base64 PNG) plus structured metadata without dataUri. Set saveToTmp=true to write snapshots into .ashfox/tmp for manual upload fallback. See ashfox://guide/vision-fallback via resources/read. Single returns result.image; sequence returns result.frames[]. Example(single): \\{\"mode\":\"fixed\",\"output\":\"single\",\"angle\":[30,45,0]\\} Example(sequence): \\{\"mode\":\"turntable\",\"output\":\"sequence\",\"durationSeconds\":2,\"fps\":12\\}"
summary: "Renders a preview image. fixed -> single (optional angle). turntable -> sequence. Returns MCP image content blocks (base64 PNG) plus structured metadata without dataUri. Set saveToTmp=true to write snapshots into..."
---

## Quick Summary
Renders a preview image. fixed -> single (optional angle). turntable -> sequence. Returns MCP image content blocks (base64 PNG) plus structured metadata without dataUri. Set saveToTmp=true to write snapshots into .ashfox/tmp for manual upload fallback. See ashfox://guide/vision-fallback via resources/read. Single returns result.image; sequence returns result.frames[]. Example(single): \{"mode":"fixed","output":"single","angle":[30,45,0]\} Example(sequence): \{"mode":"turntable","output":"sequence","durationSeconds":2,"fps":12\}

## Overview
- Tool name: `render_preview`
- Category: Project
- Strict schema: `true`

## Tool Description
Renders a preview image. fixed -> single (optional angle). turntable -> sequence. Returns MCP image content blocks (base64 PNG) plus structured metadata without dataUri. Set saveToTmp=true to write snapshots into .ashfox/tmp for manual upload fallback. See ashfox://guide/vision-fallback via resources/read. Single returns result.image; sequence returns result.frames[]. Example(single): \{"mode":"fixed","output":"single","angle":[30,45,0]\} Example(sequence): \{"mode":"turntable","output":"sequence","durationSeconds":2,"fps":12\}

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "mode"
  ],
  "additionalProperties": false,
  "properties": {
    "mode": {
      "enum": [
        "fixed",
        "turntable"
      ]
    },
    "angle": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 2,
      "maxItems": 3
    },
    "clip": {
      "type": "string"
    },
    "timeSeconds": {
      "type": "number"
    },
    "durationSeconds": {
      "type": "number"
    },
    "fps": {
      "type": "number"
    },
    "output": {
      "enum": [
        "single",
        "sequence"
      ]
    },
    "saveToTmp": {
      "type": "boolean"
    },
    "tmpName": {
      "type": "string"
    },
    "tmpPrefix": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `mode` | `enum("fixed", "turntable")` | ✅ | Selects how the tool executes this request. | enum: "fixed", "turntable" |
| `angle` | `array<number>` | ❌ | Request parameter `angle`. | minItems: 2, maxItems: 3 |
| `clip` | `string` | ❌ | Animation clip name used as the target. | No extra constraints beyond the declared type. |
| `timeSeconds` | `number` | ❌ | Request parameter `timeSeconds`. | No extra constraints beyond the declared type. |
| `durationSeconds` | `number` | ❌ | Request parameter `durationSeconds`. | No extra constraints beyond the declared type. |
| `fps` | `number` | ❌ | Request parameter `fps`. | No extra constraints beyond the declared type. |
| `output` | `enum("single", "sequence")` | ❌ | Request parameter `output`. | enum: "single", "sequence" |
| `saveToTmp` | `boolean` | ❌ | Request parameter `saveToTmp`. | No extra constraints beyond the declared type. |
| `tmpName` | `string` | ❌ | Request parameter `tmpName`. | No extra constraints beyond the declared type. |
| `tmpPrefix` | `string` | ❌ | Request parameter `tmpPrefix`. | No extra constraints beyond the declared type. |
| `includeState` | `boolean` | ❌ | Includes the latest project snapshot in the response. | No extra constraints beyond the declared type. |
| `ifRevision` | `string` | ❌ | Optimistic concurrency token used to avoid stale writes. | No extra constraints beyond the declared type. |

## Minimal Request Example
```json
{
  "mode": "fixed"
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "kind": "single",
    "frameCount": 0
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat response examples as schematic and handle optional fields defensively.
- This tool is read-only, so `ifRevision` is usually unnecessary.
